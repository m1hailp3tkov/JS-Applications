{{#if isAuthor}}
    <a href="#/edit/{{teamId}}" class="btn btn-dark">Edit Info</a>
{{/if}}
{{#if isOnTeam}}
    <a href="#/leave/{{teamId}}" class="btn btn-dark">Leave Team</a>
{{else}}
    {{#if hasNoTeam}}
        <a href="#/join/{{teamId}}" class="btn btn-dark">Join Team</a>
    {{/if}}
{{/if}}